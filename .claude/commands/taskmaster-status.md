Show Task Master project status and overview.

Steps:

1. Use `get_tasks` MCP tool to get all tasks with subtasks
2. Display project overview with task counts by status
3. Use `complexity_report` MCP tool if available to show complexity analysis
4. Use `next_task` MCP tool to highlight the next recommended task
5. Show any blocked or high-priority tasks that need attention
6. Provide a brief project health assessment